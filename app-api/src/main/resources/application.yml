spring:
  application:
    name: app

  config:
    import: optional:file:.env[.properties]

  profiles:
    include:
      - infra-external-api
    group:
      local: local, infra-db-local
      prod: prod, infra-db-prod

management:
  endpoints:
    web:
      exposure:
        include: prometheus, health